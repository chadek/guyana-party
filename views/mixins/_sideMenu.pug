mixin sideMenu()
  if user
    .menu-avatar
      a(href="/account"): img.avatar(src=(user.photo ? `/uploads/${user.photo}` : user.gravatar + '&d=retro') alt="photo de profile" title="Mon compte")
  ul
    li: a(href="/") Accueil
    if !user
      li: a(href="/login") Connexion / Inscription
    li: a(href="/events") Tous les évènements
    if user
      li: a(href="/organisms/add") Créer un organisme
      li: a(href="/logout") Déconnexion
    else
      li: a(href="#") Conditions générales d'utilisation
