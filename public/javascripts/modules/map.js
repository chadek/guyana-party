import axios from "axios";
import { $ } from "./bling";

const mapOptions = {
  center: { lat: 43.2, lng: -79.8 },
  zoom: 10
};

function loadEvents() {

}

function makeMap(mapDiv) {
  if (!mapDiv) return;
}

export default makeMap;
